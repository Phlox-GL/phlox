# 2026-08-22 16:30

- 为合并后的 Phlox Calcit 0.13.29 代码准备正式版本 `0.7.4`。
- 将 touch-control、lilac、memof、pointed-prompt 的依赖从分支/旧版本切换到正式版本号，其中 touch-control 使用新发布的 0.0.18、pointed-prompt 使用 0.0.8。
- 本地通过 caps、canonical Snapshot 格式、JS codegen、Yarn immutable 和 Vite production build；保留 npm CSS 包的既有可用版本。
