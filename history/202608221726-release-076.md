# 2026-08-22 17:26 — Release Phlox 0.7.6

- Bumped the package and `deps.cirru` versions from 0.7.5 to 0.7.6 after merging the deprecated dependency removal.
- Verified `caps --ci`, canonical Snapshot formatting, Calcit check-only, JavaScript generation, immutable Yarn install, and the Vite production build.
- The release no longer requires `lilac` or `memof`; consumers can load Phlox with its public runtime dependencies only.
